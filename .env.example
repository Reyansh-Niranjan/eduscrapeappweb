# Example environment variables for the frontend (Vite)
# Copy to `.env.local` (development) or `.env.production` (production build).

# Vite variables must be prefixed with VITE_
VITE_CONVEX_URL=https://<your-deployment-name>.convex.cloud

# Optional voice provider toggles
# - "browser" uses Web Speech APIs
# - "elevenlabs" uses Convex HTTP routes (/eleven/tts and /eleven/scribe-token)
VITE_TTS_PROVIDER=browser
VITE_STT_PROVIDER=browser
