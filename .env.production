# Production Environment Variables
# These will be used during the build process

# Vite variables must be prefixed with VITE_
VITE_APP_ENV=production
VITE_CONVEX_URL=https://adventurous-hummingbird-214.convex.cloud

# Note: The OpenRouter API key is configured server-side in Convex as:
# OPENROUTER_API_KEY
# (Do not prefix it with VITE_ and do not put it in frontend env files.)
